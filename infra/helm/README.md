# Helm charts (placeholder)

Create one chart per service or a parent chart with dependencies.
Recommended:
- values.yaml per env (dev/staging/prod)
- External secrets integration
- HPA + PDB
- NetworkPolicy
