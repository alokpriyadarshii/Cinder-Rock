# Terraform (placeholder)

Use Terraform to provision:
- VPC/VNet
- Managed Postgres
- Managed Kafka (or MSK/Confluent)
- Kubernetes cluster
- DNS + TLS certs
- Observability stack

This folder is intentionally left as a template.
